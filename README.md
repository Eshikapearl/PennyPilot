# PennyPilot
The Expense Tracker is a Python command-line application that helps users manage daily expenses. It supports adding, updating, deleting, and viewing expenses, generating monthly summaries, setting budgets with alerts, filtering by category, and exporting data to CSV using JSON for storage.
